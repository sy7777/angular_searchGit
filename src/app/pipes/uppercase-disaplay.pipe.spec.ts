import { UppercaseDisaplayPipe } from './uppercase-disaplay.pipe';

describe('UppercaseDisaplayPipe', () => {
  it('create an instance', () => {
    const pipe = new UppercaseDisaplayPipe();
    expect(pipe).toBeTruthy();
  });
});
